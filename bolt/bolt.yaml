name: "Cata"
description: "Bolt Cata"

permissions:
  owners: 'sebastian_lee2'
  viewers: 'sebastian_lee2'

command: 'source /mnt/miniconda/etc/profile.d/conda.sh && conda activate PY3 &&
export PYTHONPATH="${PYTHONPATH}:$PWD" && python experiments/main.py'
setup_command: 'bash bolt/cata_bolt_setup.sh'

resources:
  num_cpus: 4
  num_gpus: 0
  cluster: apc_usmsc39

environment_variables:
  USE_BOLT_EXPERIMENTAL_CONDA: "1"