experiment_name:    
use_gpu:                            True                      # whether to use a gpu if it is available on the device          
seed:                               0
resume:
verbose:                      

training:
  training_iterations:              250000                    # number of training iterations
  train_batch_size:                 25                        # number of tasks sampled per meta-update (per outer loop)
  test_batch_size:
  learning_rate:
  num_teachers:
  curriculum:

model:
  input_dimension:                  1                         # dimension of network input 
  student_hidden_layers:            [2]                       # dimension of hidden layers to be used in student network
  teacher_hidden_layers:            [1]                       # dimension of hidden layers to be used in teacher network(s)
  output_dimension:                 1                         # dimension of network output
  nonlinearity:
  teacher_initialisation_std:
  student_initialisation_std:
  add_noise_teacher:
  soft_comittee:
  bias_parameters:
